import React, {Component} from 'react' ;
import campingsList from './campingsList.json' ;

class CampDetails  extends Component {

    
    
    // create (function based) return with page design taking in properties from json (ex {camping.name})
    //create dummy json with 1 camp only such json we will receive via above fetch function from django
    //create  fetch function to request json with 1 camp based on the url with unique slug

    
    render () {
        return (
            <div>
                <h1>Hi Ji </h1>
            </div>
        )       
    }
}

export default CampDetails;